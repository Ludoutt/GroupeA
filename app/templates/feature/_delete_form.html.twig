<form method="post" action="{{ path('feature_delete', {'id': feature.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette tâche ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ feature.id) }}">
    <input type="hidden" name="_project" value="{{ feature.project }}">
    <button class="btn"><img class="delete" alt="delete" src="{{ asset('assets/img/delete.png') }}" /></button>
</form>
